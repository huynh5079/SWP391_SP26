@model AEMS_Solution.Models.Approver.ApproverActionFormVm

@{
    ViewData["Title"] = Model.Heading ?? "Perform Action";
    Layout = "_Approvallayout";
}

<div class="container mt-4" style="max-width: 720px;">
    <h3 class="mb-3">@Model.Heading</h3>
    <div class="card shadow-sm">
        <div class="card-body">
            <p class="text-muted">Please enter a comment for this action.</p>

            <form asp-controller="Approver" asp-action="@Model.Operation" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" name="id" value="@Model.EventId" />

                <div class="mb-3">
                    @{ var requireComment = !(Model.Operation?.Equals("Approve", StringComparison.OrdinalIgnoreCase) ?? false); }
                    <label class="form-label">Comment @(requireComment ? "(required)" : "(optional)")</label>
                    <textarea name="comment" class="form-control" rows="4" placeholder="Enter reason or notes" @(requireComment ? "required" : "")></textarea>
                </div>

                <div class="d-flex justify-content-end gap-2">
                    <a asp-controller="Approver" asp-action="Index" class="btn btn-secondary">Cancel</a>
                    <button type="submit" class="btn btn-primary">Submit</button>
                </div>
            </form>
        </div>
    </div>
</div>
