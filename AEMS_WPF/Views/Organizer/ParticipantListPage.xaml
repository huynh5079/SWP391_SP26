<Page x:Class="AEMS_WPF.Views.Organizer.ParticipantListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AEMS_WPF.Views.Organizer"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="Participant List">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,30">
            <StackPanel>
                <TextBlock x:Name="txtEventTitle" Text="Event Participants" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                <TextBlock Text="Manage registration status, attendance, and waitlists for this event." Foreground="{StaticResource TextMutedBrush}" FontSize="14"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="Scan QR" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0" Padding="15,8" Click="BtnScanQR_Click"/>
                <Button Content="Back" Background="Transparent" Foreground="{StaticResource TextMutedBrush}" BorderThickness="0" Click="BtnBack_Click"/>
            </StackPanel>
        </Grid>

        <!-- Search & Filter -->
        <Border Grid.Row="1" Background="{StaticResource LowWhiteBrush}" CornerRadius="12" Padding="15" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox x:Name="txtSearch" Style="{StaticResource ModernTextBoxStyle}" Tag="Search by name or email..." Margin="0,0,15,0"/>
                
                <ComboBox Grid.Column="1" x:Name="cbStatusFilter" SelectedIndex="0" Margin="0,0,15,0">
                    <ComboBoxItem Content="All Statuses"/>
                    <ComboBoxItem Content="Registered"/>
                    <ComboBoxItem Content="Attended"/>
                    <ComboBoxItem Content="Waitlisted"/>
                    <ComboBoxItem Content="Cancelled"/>
                </ComboBox>
                
                <Button Grid.Column="2" Content="Export CSV" Background="{StaticResource LowWhiteBrush}" Foreground="White" Padding="15,8" BorderThickness="0"/>
            </Grid>
        </Border>

        <!-- List -->
        <Border Grid.Row="2" Background="{StaticResource LowWhiteBrush}" CornerRadius="15" Padding="10">
            <DataGrid x:Name="dgParticipants" AutoGenerateColumns="False" 
                      Background="Transparent" BorderThickness="0" 
                      Foreground="White" GridLinesVisibility="None"
                      CanUserAddRows="False" IsReadOnly="True"
                      RowBackground="Transparent">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="15,10"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="{StaticResource LowWhiteBrush}"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Student" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5">
                                    <Border Width="30" Height="30" CornerRadius="15" Background="{StaticResource LowWhiteBrush}" Margin="0,0,10,0">
                                        <TextBlock Text="{Binding FullName[0]}" Foreground="White" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel>
                                        <TextBlock Text="{Binding FullName}" Foreground="White" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Email}" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <DataGridTextColumn Header="MSSV" Binding="{Binding StudentId}" Width="100"/>
                    
                    <DataGridTemplateColumn Header="Status" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource LowWhiteBrush}" CornerRadius="10" Padding="8,3" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Status}" FontSize="12" Foreground="White"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <DataGridTextColumn Header="Check-in Time" Binding="{Binding CheckInTime, StringFormat='HH:mm dd/MM'}" Width="150"/>
                    
                    <DataGridTemplateColumn Header="Actions" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Check-in" Click="BtnCheckIn_Click" Background="Transparent" Foreground="#34d399" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0"/>
                                    <Button Content="Remove" Click="BtnRemove_Click" Background="Transparent" Foreground="#f87171" BorderThickness="0" Cursor="Hand"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
