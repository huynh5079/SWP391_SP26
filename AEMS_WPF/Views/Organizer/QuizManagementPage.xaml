<Page x:Class="AEMS_WPF.Views.Organizer.QuizManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:enums="clr-namespace:DataAccess.Enum;assembly=DataAccess"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1000"
      Title="Quiz Management">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="30">
            <StackPanel>
                <TextBlock x:Name="txtEventTitle" Text="Event Quiz Management" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                <Button Content="Back to Details" Background="Transparent" Foreground="Gray" BorderThickness="0" Click="BtnBack_Click" HorizontalAlignment="Left"/>
            </StackPanel>
            <Button Content="+ Create New Quiz" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Top" Click="BtnCreateQuiz_Click"/>
        </Grid>

        <!-- Quiz List -->
        <ScrollViewer Grid.Row="1" Margin="30,0,30,30">
            <ItemsControl x:Name="icQuizzes">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource LowWhiteBrush}" CornerRadius="10" Padding="20" Margin="0,0,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,5"/>
                                    <StackPanel Orientation="Horizontal">
                                        <Border Background="#33fbbf24" CornerRadius="4" Padding="6,2" Margin="0,0,10,0">
                                            <TextBlock Text="{Binding Type}" Foreground="#fbbf24" FontSize="11" FontWeight="Bold"/>
                                        </Border>
                                        <TextBlock Text="{Binding QuestionCount, StringFormat='{}{0} Questions'}" Foreground="Gray" FontSize="13" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Content="View Scores" Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0" Click="BtnViewScores_Click"/>
                                    <Button x:Name="BtnListItemPublish" Content="Publish" Click="BtnPublish_Click" Margin="0,0,10,0">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Draft">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
